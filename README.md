# Sample-FTP-Server
A FTP server supports the following three basic operations: • DOWNLOAD: the client can download a file from the server; • DELETE: the client can delete a file in the server; • RENAME: the client can rename a file in the server
